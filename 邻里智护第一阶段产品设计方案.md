# 邻里智护第一阶段产品设计方案

## AI 开发交付版

- 产品名称：邻里智护
- 产品副标题：面向完整社区“一老一小”的照护事件闭环与应急协同平台
- 文档版本：V1.0
- 交付目标：做出一个无需硬件、无需外部服务、可以本地运行并完整演示的第一阶段软件产品
- 领域词汇：见 [CONTEXT.md](C:\VScode项目\国创赛资料\CONTEXT.md)

## 0. 给开发 AI 的总要求

请把本项目实现成一个“小而完整”的社区照护事件系统，不要扩展成智慧城市平台、医疗系统或物联网平台。

第一阶段必须满足：

1. 所有数据可以由种子数据、人工录入或模拟按钮产生。
2. 不依赖摄像头、手环、定位器、门禁、MQTT、Zigbee、短信、微信、真实电话或云服务。
3. 重点实现“发现—分级—通知—派单—接单—处置—升级—确认—关闭”闭环。
4. 每次关键操作都要留下时间线记录，页面刷新后数据仍然存在。
5. 风险分级必须能展示原因，不能只显示一个无法解释的“AI 结果”。
6. 使用虚构演示数据，不采集和展示真实身份证号、精确住址及详细病历。
7. 不做医疗诊断。页面文案使用“疑似异常”“建议人工确认”，不得使用“确诊”“百分百识别”等表述。

## 1. 产品定义

### 1.1 一句话定位

邻里智护是一个面向社区工作人员的“一老一小”照护事件协同工作台，帮助家属、社区、物业、社工和志愿者共同完成风险事件的发现、响应和闭环。

### 1.2 要解决的问题

当前社区照护中的问题不是“没有任何人”，而是：

- 家属、社区和物业之间信息分散在电话和聊天群里；
- 异常发生后没有明确责任人；
- 事件容易被遗漏，超时后也没有自动提醒；
- 处理结果没有统一记录，家属无法确认事情是否真正解决；
- 老人照护和儿童接送通常由不同人员负责，缺少统一工作台。

### 1.3 产品价值

对家属：知道家人发生了什么、谁正在处理。

对社区：知道哪些事情最紧急、当前由谁负责、哪些任务已超时。

对服务人员：知道任务内容、处理边界和反馈方式。

对比赛展示：用同一套事件引擎同时演示老人和儿童场景，形成可见、可操作、可统计的闭环。

## 2. 第一阶段范围

### 2.1 必须实现（P0）

- 演示账号登录和角色切换；
- 老人/儿童照护对象档案；
- 家属/监护关系和风险标签；
- 六类照护事件的人工创建和模拟创建；
- P0、P1、P2 风险分级；
- 事件列表、筛选、排序和搜索；
- 通知家属/监护人和社区响应人；
- 派单、接单、转派；
- 处置记录和事件时间线；
- 未接单或未反馈时的超时升级；
- 家属确认关闭或授权社区响应人关闭；
- 事件统计看板；
- SQLite 本地数据持久化；
- 一键恢复演示数据。

### 2.2 明确不做

- 真实摄像头跌倒检测；
- 手环、定位器、门禁等硬件接入；
- 医疗诊断、病历管理、在线问诊；
- 120、110、短信、微信和电话真实对接；
- 社区商城、缴费、论坛、家政交易；
- 复杂大模型、计算机视觉和语音识别；
- 多租户、微服务、区块链和复杂云部署；
- 真实居民身份认证和敏感个人信息管理。

## 3. 用户角色与权限

第一阶段使用预置演示账号。角色切换只用于演示不同视角，不要求实现复杂注册体系。

| 角色 | 可以查看 | 可以执行 | 不能执行 |
|---|---|---|---|
| 家属/监护人 | 自己被授权的照护对象、相关事件和处理进度 | 平安签到、发起求助、补充说明、确认关闭 | 查看其他家庭信息、修改社区规则 |
| 社区工作人员 | 本社区全部照护对象和事件 | 分级、派单、接单、转派、记录处置、升级、关闭 | 查看无关社区数据 |
| 物业/社工/志愿者 | 分配给自己的事件和完成任务所需的最少信息 | 接单、反馈、请求转派 | 修改照护对象档案、查看全部事件 |
| 社区管理员 | 全部演示数据和统计 | 管理人员、资源、事件规则和演示数据 | 无特殊限制，仅用于演示 |

权限原则：家属看“我的家庭”，响应人看“我的任务”，社区工作人员看“我的社区”。

## 4. 核心业务对象

### 4.1 照护对象

可以被照护的老人或儿童。第一阶段只允许两种类型：`ELDER` 老人、`CHILD` 儿童。

核心字段：

- 编号、姓名、类型、年龄、性别；
- 脱敏联系电话；
- 小区/楼栋等模糊位置；
- 所属家庭；
- 风险标签；
- 最近一次平安签到时间；
- 当前是否有未关闭事件。

### 4.2 家属/监护关系

记录谁负责关注照护对象，以及能够执行什么操作。

关系类型：`FAMILY` 家属、`GUARDIAN` 监护人、`PICKUP_AUTHORIZED` 接送授权人。

### 4.3 照护事件

一次需要跟踪处理的异常、求助或服务需求，是整个系统的核心对象。

事件来源：

- `MANUAL`：工作人员或家属人工创建；
- `SCHEDULED_CHECKIN`：根据签到计划生成；
- `SIMULATOR`：演示用模拟按钮生成。

事件状态：

| 状态 | 含义 |
|---|---|
| `PENDING` 待处理 | 已创建，尚未形成当前责任人 |
| `ASSIGNED` 已派单 | 已分配给响应人，等待接单 |
| `IN_PROGRESS` 处理中 | 已有人接单或已经开始处置 |
| `WAITING_CONFIRM` 待确认 | 处置已完成，等待家属或授权人员确认 |
| `CLOSED` 已关闭 | 已完成确认，事件生命周期结束 |
| `CANCELLED` 已取消 | 误报、重复或经授权取消 |

“已升级”不是单独状态，而是事件上的升级等级和时间线记录。这样可以在事件仍处于“处理中”时继续升级。

### 4.4 照护任务

照护事件被分配给某个响应人后形成的责任记录。一个事件可以有多条历史任务，但同时只能有一个当前责任人。

### 4.5 处置记录

对事件采取的实际动作，例如电话联系、到场查看、协助购买药品、联系监护人、转派或误报说明。

### 4.6 关闭确认

家属/监护人确认“已经解决”，或者授权社区工作人员填写关闭原因并关闭。关闭必须留下确认人、确认时间和关闭说明。

## 5. 事件类型与默认规则

第一阶段只实现以下六种事件。事件名称要使用下面的固定枚举，避免前后端名称不一致。

| 枚举 | 中文名称 | 默认等级 | 默认响应链 |
|---|---|---|---|
| `ELDER_MISSED_CHECKIN` | 老人长时间未签到 | P1 | 家属/监护人 → 社区工作人员 → 社工/志愿者 |
| `ELDER_HELP` | 老人主动求助 | P1；选择紧急时 P0 | 家属/监护人 + 社区工作人员 |
| `ELDER_SUSPECTED_FALL` | 疑似老人跌倒或突发异常 | P1；长时间未确认时 P0 | 家属/监护人 + 社区工作人员 → 社工/志愿者 |
| `CHILD_PICKUP_TIMEOUT` | 儿童接送未确认 | P1 | 监护人 → 托管/社区工作人员 |
| `CHILD_CARE_CHECKIN_ABNORMAL` | 儿童托管签到异常 | P1 | 监护人 → 托管/社区工作人员 |
| `CHILD_HELP` | 儿童主动求助 | P0 | 监护人 + 社区工作人员 |

`ELDER_HELP` 的服务内容可以选择：助餐、助洁、陪同、代办、其他。第一阶段不做服务交易，只记录需求和处置结果。

## 6. 风险分级设计

### 6.1 分级规则

风险分级必须透明、可解释、可在详情页展开查看。

基础分：

| 事件默认等级 | 基础分 |
|---|---:|
| P0 | 90 |
| P1 | 60 |
| P2 | 30 |

附加分：

- 独居、高龄、失能、无人监护等高风险标签：每项 `+10`，最多加 `20`；
- 家属/监护人未响应：`+15`；
- 超过响应时限：`+10`；
- 已完成平安签到或人工确认安全：`-20`。

计算结果：

- 分数 `>= 80`：P0；
- 分数 `50–79`：P1；
- 分数 `< 50`：P2；
- 不得低于事件类型的最低等级。例如“儿童主动求助”最低为 P0。

页面必须显示类似以下文案：

```text
风险等级：P1 高风险
风险分：75
计算原因：老人长时间未签到（60）+ 独居（10）+ 高龄（10）- 已有家属确认（5）
```

### 6.2 响应时限

正式配置使用分钟：

| 等级 | 首次接单时限 | 升级时限 |
|---|---:|---:|
| P0 | 1 分钟 | 3 分钟 |
| P1 | 5 分钟 | 10 分钟 |
| P2 | 30 分钟 | 60 分钟 |

为了方便比赛演示，页面提供“模拟超时”按钮，直接把事件推进到超时状态并生成升级记录，不需要现场等待真实时间。

## 7. 核心业务流程

### 7.1 老人未签到流程

1. 根据老人档案中的签到计划，或在模拟事件台点击“老人 4 小时未签到”。
2. 系统创建 `ELDER_MISSED_CHECKIN` 事件。
3. 根据“独居、高龄”等标签计算风险等级。
4. 通知家属/监护人，事件进入 `ASSIGNED`。
5. 家属可以点击“已确认平安”，也可以点击“无法处理”。
6. 家属未响应或点击“无法处理”时，事件升级给社区工作人员。
7. 社区工作人员接单，填写电话联系或上门查看。
8. 处置完成后进入 `WAITING_CONFIRM`。
9. 家属点击“确认已解决”，事件进入 `CLOSED`。

### 7.2 儿童接送未确认流程

1. 工作人员或模拟事件台创建儿童接送事件，填写约定接送时间。
2. 超过约定时间仍没有接送确认，系统创建 P1 事件。
3. 通知监护人和托管工作人员。
4. 托管工作人员接单，联系监护人并记录接送结果。
5. 监护人确认孩子已安全接到，事件关闭。

### 7.3 主动求助流程

1. 家属或工作人员选择照护对象并提交求助类型、说明和紧急程度。
2. 紧急求助直接生成 P0；普通服务需求生成 P2。
3. P0 同时通知家属/监护人和社区工作人员。
4. 社区工作人员接单后，可以转派给物业、社工或志愿者。
5. 处置记录完成后，由家属或授权社区工作人员关闭。

## 8. 页面与交互设计

### 8.1 演示登录页 `/login`

页面内容：

- 产品名称和一句话定位；
- 四个演示角色卡片：家属、社区工作人员、社区响应人、管理员；
- 点击角色直接进入系统，不需要真实密码；
- 页面下方标注“演示数据为虚构数据”。

登录后顶部一直显示当前角色，并提供角色切换入口。

### 8.2 社区事件中心 `/events`

顶部指标卡：待处理、处理中、已超时、今日已关闭。

筛选条件：

- 状态；
- 风险等级；
- 老人/儿童；
- 事件类型；
- 当前责任人；
- 关键词搜索姓名或事件编号。

列表字段：

- 事件编号；
- 事件类型；
- 照护对象姓名和年龄；
- 老人/儿童标签；
- 风险等级和风险分；
- 当前状态；
- 当前责任人；
- 创建时间和剩余响应时间；
- 操作：查看、接单、转派、模拟超时。

交互要求：

- P0 使用红色，P1 使用橙色，P2 使用蓝色；
- 超时事件显示“已超时”标签和升级次数；
- 点击一行进入事件详情；
- 无数据时显示引导，不显示空白页面。

### 8.3 照护对象列表 `/people`

列表字段：姓名、类型、年龄、家庭、风险标签、最近签到、未关闭事件数。

操作：查看档案、记录平安签到、发起照护事件。

### 8.4 照护对象详情 `/people/:id`

分为三块：

1. 基本信息：姓名、年龄、家庭、脱敏联系方式；
2. 照护信息：风险标签、家属/监护人、签到计划、接送授权人；
3. 历史记录：最近签到和历史照护事件。

页面按钮：

- “记录平安签到”；
- “发起求助”；
- “查看未关闭事件”。

### 8.5 模拟事件台 `/simulator`

这是第一阶段的重要页面，用于证明产品不依赖硬件也能运行。

快捷按钮：

- 模拟老人 4 小时未签到；
- 模拟老人主动求助；
- 模拟疑似跌倒；
- 模拟儿童接送超时；
- 模拟托管签到异常；
- 模拟儿童主动求助；
- 模拟家属未响应并立即升级；
- 重置全部演示数据。

每个按钮点击后：

1. 创建一条真实照护事件记录；
2. 自动计算风险等级；
3. 自动生成通知和初始处置记录；
4. 跳转到事件详情页；
5. 用户可以继续接单、处置和关闭。

### 8.6 事件详情 `/events/:id`

建议采用“左侧时间线 + 右侧事件信息和操作区”的布局。

左侧时间线显示：

- 事件创建；
- 风险分级及原因；
- 通知对象；
- 派单、接单、转派；
- 电话联系、到场查看和其他处置；
- 超时和升级；
- 家属确认和关闭。

右侧信息卡显示：

- 事件编号、类型、风险等级；
- 照护对象和风险标签；
- 当前责任人；
- 响应倒计时/超时状态；
- 联系家属按钮；
- 操作按钮。

不同角色显示不同操作：

- 家属：平安签到、确认已解决、补充说明；
- 社区工作人员：接单、转派、记录处置、升级、关闭；
- 响应人：接单、反馈、请求转派；
- 管理员：以上全部操作和重置演示数据。

### 8.7 社区看板 `/dashboard`

只展示能帮助社区决策的指标：

- 今日事件总数；
- P0/P1/P2 数量；
- 平均首次响应时间；
- 超时率；
- 老人和儿童事件占比；
- 事件类型分布；
- 最近七天关闭趋势。

图表不超过四个，避免做成只有数字没有业务意义的大屏。

## 9. 事件操作规则

### 9.1 接单

- 只有当前被派单的响应人，或社区工作人员，能够接单；
- 接单后状态从 `ASSIGNED` 变为 `IN_PROGRESS`；
- 系统写入“某人已接单”的处置记录；
- 接单时间停止首次接单倒计时，但仍保留升级倒计时。

### 9.2 转派

- 必须选择新的响应人或社区资源；
- 必须填写转派原因；
- 原任务标记为已转派，新任务成为当前任务；
- 时间线记录原责任人、新责任人、原因和时间。

### 9.3 处置反馈

处置类型固定为：

- `CALL` 电话联系；
- `ARRIVE_CHECK` 到场查看；
- `ASSIST_SERVICE` 协助服务；
- `CONTACT_GUARDIAN` 联系家属/监护人；
- `FALSE_REPORT` 误报说明；
- `OTHER` 其他。

每条反馈至少包含处置类型、说明和操作人，说明不能为空。

### 9.4 超时升级

- 超过首个响应时限仍没有接单：通知社区工作人员；
- 超过升级时限仍没有处置反馈：通知下一级社区资源；
- 每次升级生成一条处置记录和一条站内通知；
- 事件状态不强制改为“升级中”，继续保持 `ASSIGNED` 或 `IN_PROGRESS`；
- P0 事件不允许被普通响应人降级。

### 9.5 关闭

- 进入关闭前必须至少有一条处置反馈；
- 家属确认关闭时记录确认人和确认时间；
- 社区工作人员强制关闭时必须选择关闭原因；
- 已关闭事件只读，不允许继续派单或修改风险等级；
- 误报使用“取消”而不是删除，保留审计记录。

## 10. 数据模型

第一阶段使用 SQLite。建议至少包含以下表或等价的数据结构：

### `users` 用户

`id`、`name`、`role`、`phone_masked`、`community_id`、`active`。

`role`：`FAMILY`、`COMMUNITY_WORKER`、`RESPONDER`、`ADMIN`。

### `care_subjects` 照护对象

`id`、`name`、`subject_type`、`age`、`gender`、`household_id`、`location_text`、`last_check_in_at`、`active`。

### `households` 家庭

`id`、`name`、`building_text`、`community_id`。

### `relationships` 关系

`id`、`subject_id`、`user_id`、`relationship_type`、`can_confirm`、`can_view`、`can_pickup`。

### `risk_profiles` 风险档案

`id`、`subject_id`、`tags`、`check_in_enabled`、`check_in_interval_hours`、`pickup_plan_text`、`notes_safe`。

### `care_events` 照护事件

`id`、`event_no`、`subject_id`、`event_type`、`source`、`risk_level`、`risk_score`、`risk_reasons`、`status`、`description`、`occurred_at`、`created_at`、`first_response_due_at`、`escalation_due_at`、`current_assignee_id`、`escalation_level`、`closed_at`、`closed_by`、`close_reason`。

### `assignments` 照护任务

`id`、`event_id`、`assignee_id`、`resource_id`、`assignment_level`、`status`、`assigned_at`、`accepted_at`、`transferred_at`、`transfer_reason`。

### `action_logs` 处置记录

`id`、`event_id`、`actor_id`、`action_type`、`content`、`created_at`。

### `notifications` 站内通知

`id`、`recipient_id`、`event_id`、`notification_type`、`title`、`content`、`read_at`、`created_at`。

## 11. API 约定

采用 REST API。接口名称可以按团队习惯调整，但业务含义不要改变。

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/demo-login` | 演示角色登录 |
| `GET` | `/api/people` | 查询照护对象 |
| `GET` | `/api/people/{id}` | 查看照护对象详情 |
| `POST` | `/api/people/{id}/check-in` | 记录平安签到 |
| `GET` | `/api/events` | 查询事件列表，支持筛选 |
| `POST` | `/api/events` | 创建照护事件 |
| `GET` | `/api/events/{id}` | 查看事件详情和时间线 |
| `POST` | `/api/events/{id}/assign` | 派单或转派 |
| `POST` | `/api/events/{id}/accept` | 接单 |
| `POST` | `/api/events/{id}/actions` | 添加处置记录 |
| `POST` | `/api/events/{id}/simulate-timeout` | 模拟超时并升级 |
| `POST` | `/api/events/{id}/confirm-close` | 家属确认关闭 |
| `POST` | `/api/events/{id}/close` | 授权社区人员关闭 |
| `GET` | `/api/notifications` | 查询站内通知 |
| `PATCH` | `/api/notifications/{id}/read` | 标记通知已读 |
| `GET` | `/api/dashboard/summary` | 获取看板指标 |
| `POST` | `/api/demo/reset` | 恢复种子数据 |

创建事件的请求示例：

```json
{
  "subject_id": "SUBJECT-001",
  "event_type": "ELDER_MISSED_CHECKIN",
  "source": "SIMULATOR",
  "description": "老人连续 4 小时未提交平安签到",
  "occurred_at": "2026-09-04T10:00:00+08:00"
}
```

创建事件后，后端一次性完成：保存事件、计算风险、建立初始任务、生成站内通知和写入首条时间线记录。

## 12. 演示种子数据

所有姓名、联系方式和地址均为虚构数据。

### 照护对象

- `SUBJECT-001` 李秀梅，78 岁，老人，标签：独居、高龄；家属：李晨；
- `SUBJECT-002` 王建国，82 岁，老人，标签：高龄；家属：王芳；
- `SUBJECT-003` 陈小雨，8 岁，儿童；监护人：陈琳；接送授权人：赵老师；
- `SUBJECT-004` 周子涵，6 岁，儿童；监护人：周明；托管点：阳光托管班。

### 演示账号

- 家属：李晨；
- 社区工作人员：赵敏；
- 社区响应人：张师傅；
- 管理员：社区管理员。

### 初始事件

- 李秀梅：老人长时间未签到，P1，待社区响应；
- 陈小雨：儿童接送未确认，P1，已派给监护人；
- 王建国：助餐需求，P2，已关闭。

## 13. 推荐技术实现

这是实现建议，不是产品功能要求：

- 前端：Vue 3、TypeScript、Vite、Element Plus、ECharts；
- 后端：FastAPI、SQLModel 或 SQLAlchemy；
- 数据库：SQLite；
- 通知：站内通知、弹窗和页面未读数；
- 开发环境：前后端本地运行，必要时由后端托管前端构建产物；
- 外部依赖：尽量为零，启动后即可使用种子数据。

可以使用定时任务处理真实倒计时，但必须保留“模拟超时”按钮，保证比赛现场不依赖等待和网络。

## 14. 验收标准

完成第一阶段的标准不是“页面数量多”，而是下面的流程能够从头到尾跑通：

1. 启动系统后能看到虚构居民和初始事件；
2. 切换到社区工作人员角色，能筛选出 P1 未签到事件；
3. 点击模拟事件后，事件出现在列表中，并显示风险等级和计算原因；
4. 能看到通知对象和当前责任人；
5. 家属未响应时，点击模拟超时，事件生成升级记录和通知；
6. 社区工作人员能接单、添加电话联系和到场查看记录；
7. 事件能进入“待确认”，家属能确认关闭；
8. 关闭后事件只读，时间线完整保留；
9. 创建儿童接送未确认事件时，能复用同样的派单、升级和关闭流程；
10. 看板中的事件总数、关闭数、响应时间和超时率会随操作更新；
11. 重置演示数据后，系统回到初始状态；
12. 没有硬件、外部账号或网络服务时，核心流程仍然可以演示。

## 15. 开发顺序

建议按下面顺序交给 AI 开发，每一步都先保证能运行：

### 第 1 步：基础工程和领域模型

完成数据库表、枚举、种子数据、演示登录和基础布局。

### 第 2 步：照护对象和事件中心

完成居民档案、事件列表、事件创建、筛选和详情页。

### 第 3 步：事件闭环

完成风险分级、派单、接单、转派、处置记录和时间线。

### 第 4 步：超时升级和关闭确认

完成模拟超时、通知、升级链、家属确认和授权关闭。

### 第 5 步：看板和演示打磨

完成统计指标、空状态、错误提示、重置数据和三分钟演示路径。

## 16. 交给 AI 开发时可以直接使用的任务描述

```text
请根据《邻里智护第一阶段产品设计方案》实现一个本地可运行的“一老一小”照护事件协同平台。

严格限制范围：第一阶段只做照护对象档案、六类照护事件、P0/P1/P2 风险分级、事件列表、派单、接单、转派、处置时间线、模拟超时升级、家属确认关闭和统计看板。不要实现摄像头、手环、定位器、门禁、MQTT、Zigbee、短信、微信、在线问诊或真实医疗功能。

请先实现数据库模型、种子数据和后端接口，再实现页面。风险等级必须展示可解释的计算原因；事件的每个关键动作必须写入时间线；所有数据使用虚构数据。完成后按照文档中的验收标准逐条自测，并保证点击“模拟老人 4 小时未签到”可以在三分钟内演示完整闭环。
```


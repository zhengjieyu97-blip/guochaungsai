# 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。

- 每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

- 每次项目开发完成后，必须将本次变更提交并推送到 GitHub 的 `guochaungsai` 仓库。默认使用远程 `origin` 和当前工作分支；当前仓库远程地址为 `https://github.com/zhengjieyu97-blip/guochaungsai.git`。推送后必须验证本地与远程提交一致；如果推送失败，必须明确告知用户，不能只停留在本地 commit。

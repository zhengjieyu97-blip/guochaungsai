# 邻里智护第一阶段技术架构方案

## 文档信息

- **产品名称**：邻里智护
- **产品定位**：面向完整社区“一老一小”的照护事件闭环与应急协同平台
- **文档版本**：V1.0
- **文档用途**：交付给开发 AI 和开发团队，作为第一阶段正式实现的技术约束
- **适用范围**：本地可运行、无需硬件和外部服务的比赛演示版及其可扩展基础
- **上游文档**：[第一阶段产品设计方案](./邻里智护第一阶段产品设计方案.md)、[领域上下文](./CONTEXT.md)

> 本文描述正式系统的目标架构。`prototype/` 下的 HTML、CSS 和 JavaScript 只是交互原型与业务参考，不是正式前端实现，也不要求继承其视觉样式或代码结构。

## 0. 交付给 AI 的总指令

开发前必须先阅读：

1. `CONTEXT.md`，统一业务术语；
2. `邻里智护第一阶段产品设计方案.md`，确认产品范围、角色、事件类型、数据模型、API 和验收标准；
3. 本文，确认正式技术架构和实现顺序。

实现时必须遵守：

- 先实现数据库模型、迁移、种子数据和后端接口，再实现页面；
- 所有事件状态变化必须由后端事件模块完成，前端不得直接修改业务状态；
- 风险分级必须展示可解释的计算原因；
- 每个关键操作必须写入时间线或处置记录；
- 第一阶段使用预置账号实现**演示登录**，不实现开放注册；
- 所有演示数据为虚构数据，联系方式和位置必须脱敏；
- 保留“模拟事件”和“模拟超时”能力，但不接入真实硬件或外部消息服务；
- 每完成一个功能都要补充相关测试，并保持项目可以本地启动。

明确禁止：

- 不把当前 `prototype/styles.css`、`prototype/index.html` 或 `prototype/app.js` 直接升级成正式系统；
- 不实现摄像头、手环、门禁、定位器、MQTT、Zigbee、短信、微信、真实电话或医疗诊断；
- 不引入微服务、消息队列、Kubernetes、区块链或复杂多租户体系；
- 不把风险分级描述为医疗结论或绝对安全保证。

## 1. 架构总览

### 1.1 架构风格

采用**前后端分离的模块化单体**：

- 前端负责页面、表单、查询展示和交互状态；
- 后端负责身份、权限、领域规则、状态机、事务和持久化；
- 各业务模块在同一个后端进程中部署，通过清晰的模块接口协作；
- 不使用外部消息队列，通知先落在数据库中；
- 真实设备未来通过适配器转换为统一的照护事件，不改变核心闭环。

```text
┌─────────────────────────────────────────────┐
│                Vue 3 Web 前端                │
│ 登录、事件中心、对象档案、模拟台、看板、详情 │
└──────────────────────┬──────────────────────┘
                       │ REST/JSON
┌──────────────────────▼──────────────────────┐
│              FastAPI 模块化单体               │
│ 认证与权限                                    │
│ 照护对象与关系                                │
│ 照护事件引擎                                  │
│ 风险策略、派单、升级、时间线、通知、看板      │
│ 模拟器与种子数据                              │
└──────────────────────┬──────────────────────┘
                       │ SQLAlchemy/SQLModel
┌──────────────────────▼──────────────────────┐
│                   SQLite                      │
│   事件、任务、处置、关系、通知、审计数据       │
└─────────────────────────────────────────────┘
```

### 1.2 为什么选择模块化单体

- 第一阶段的核心难点是照护事件闭环，不是高并发；
- 单体部署更适合比赛现场，启动和排错成本最低；
- 一个数据库事务可以同时更新事件、任务、时间线和通知；
- 模块接口清晰后，未来仍可把设备接入、通知发送或统计查询拆出；
- 避免过早引入微服务、消息一致性和分布式部署问题。

### 1.3 部署形态

开发时可以前后端分别启动；交付时前端构建产物由 FastAPI 托管，做到无需外部服务即可启动：

```text
启动命令 → FastAPI → 提供 REST API 和前端静态文件 → SQLite 文件
```

建议提供一个 `start.ps1` 或等价启动脚本，完成依赖检查、数据库初始化、种子数据和服务启动。

## 2. 第一阶段范围

### 2.1 必须实现

- 独立的 `/login` 演示登录页；
- 四个预置角色：家属/监护人、社区工作人员、社区响应人、管理员；
- 老人和儿童照护对象档案；
- 家属、监护人、接送授权人关系；
- 六类照护事件的人工创建和模拟创建；
- P0、P1、P2 风险分级及计算原因；
- 事件查询、筛选、排序、搜索和详情；
- 派单、接单、转派、处置反馈；
- 超时升级和站内通知；
- 平安签到、家属确认关闭、授权社区人员关闭；
- 事件时间线和关闭后的只读历史；
- 社区统计看板；
- SQLite 持久化、种子数据和一键重置；
- 后端单元测试、API 集成测试和关键流程端到端测试。

### 2.2 第一阶段不做

- 用户自主注册、密码找回和实名认证；
- 真实居民身份认证；
- 摄像头跌倒识别、医疗诊断或生命体征判断；
- 手环、定位器、门禁、MQTT、Zigbee 等设备接入；
- 短信、微信、电话和 120/110 对接；
- 社区商城、缴费、论坛、家政交易和在线问诊；
- 多租户、微服务、复杂云部署和区块链。

## 3. 技术选型

| 层次 | 技术 | 约束 |
|---|---|---|
| 前端框架 | Vue 3 + TypeScript + Vite | 正式页面重新设计，不复制原型 CSS |
| 前端路由 | Vue Router | 路由与页面职责对应 |
| 前端状态 | Pinia | 只存会话、筛选和页面状态，不承担领域规则 |
| UI 基础 | Element Plus 或等价组件库 | 统一表单、弹窗、表格和反馈组件 |
| 图表 | ECharts | 只展示与业务决策相关的指标 |
| 后端框架 | FastAPI | 提供 REST/JSON 接口和 OpenAPI 文档 |
| 数据校验 | Pydantic 2 | 请求、响应和命令模型分离 |
| ORM/数据库访问 | SQLAlchemy 2 或 SQLModel | 通过 Repository Adapter 隔离持久化细节 |
| 迁移 | Alembic | 所有表结构变更必须有迁移 |
| 数据库 | SQLite | 第一阶段唯一必需数据库；保持未来切换 PostgreSQL 的可能性 |
| 定时任务 | APScheduler 或等价轻量调度器 | 单进程运行，模拟超时仍必须保留 |
| 后端测试 | pytest | 领域规则、接口和事务测试 |
| 前端测试 | Vitest | 组件和状态测试 |
| 端到端测试 | Playwright | 覆盖老人和儿童闭环 |

不为第一阶段引入 Redis、Celery、Kafka、RabbitMQ 或云厂商服务。

## 4. 后端模块与接口

### 4.1 模块划分

```text
backend/
└── app/
    ├── main.py
    ├── config/             # 配置和环境变量
    ├── db/                 # 引擎、会话、迁移入口
    ├── auth/               # 演示登录、会话、角色权限
    ├── subjects/           # 老人、儿童、家庭、关系
    ├── risk/               # 风险计算和响应时限策略
    ├── events/             # 照护事件聚合和状态机
    ├── assignments/        # 派单、接单、转派、升级
    ├── action_logs/        # 处置记录和时间线
    ├── notifications/      # 站内通知和已读状态
    ├── dashboard/          # 只读统计查询
    ├── demo/               # 种子、模拟场景、重置
    └── shared/             # 错误、时钟、分页、审计工具
```

每个模块尽量包含以下内部层次：

```text
routes.py       # HTTP 适配器，只负责协议和鉴权
schemas.py      # 请求、响应和命令数据结构
service.py      # 用例接口，编排事务
domain.py       # 领域规则和不变量
repository.py   # 持久化接口
models.py       # ORM 映射
```

### 4.2 核心深模块：照护事件引擎

事件引擎是整个系统的核心模块。它对外提供少量命令接口，把状态流转、权限检查、风险更新、任务更新、时间线和通知生成隐藏在内部。

建议的命令接口：

```text
create_event(command)
accept_event(event_id, actor)
transfer_event(event_id, assignee_id, reason, actor)
record_action(event_id, action_type, content, complete, actor)
record_check_in(event_id, actor)
simulate_timeout(event_id, actor)
confirm_close(event_id, reason, actor)
force_close(event_id, reason, actor)
cancel_event(event_id, reason, actor)
```

每个命令必须：

1. 在后端重新读取事件和当前用户；
2. 检查角色、关系和状态不变量；
3. 在一个数据库事务中更新相关记录；
4. 写入系统时间线或处置记录；
5. 生成需要的站内通知；
6. 返回更新后的事件摘要或详情。

前端不得通过传入 `status`、`risk_level`、`current_assignee_id` 等字段来要求后端直接覆盖状态。

### 4.3 必须维护的状态不变量

- `CLOSED` 和 `CANCELLED` 事件只读；
- 只有当前责任人或社区工作人员可以接单；
- 只有有权限的操作者可以记录处置；
- 转派必须有新责任人和原因；
- 关闭前至少存在一条处置反馈，家属确认平安也必须留下记录；
- 家属只能确认自己被授权的照护对象；
- 普通响应人不能把 P0 事件降为更低等级；
- 一个事件同时只有一个当前责任人，历史转派必须保留；
- 每次升级必须有升级次数、时间线记录和通知；
- 事件风险原因不能由浏览器提交的结果直接信任。

## 5. 领域模型与数据库

第一阶段使用 SQLite。表名可按团队习惯调整，但业务含义和字段不能丢失。

### 5.1 主要表

#### `users` 用户

- `id`：主键
- `name`
- `role`：`FAMILY`、`COMMUNITY_WORKER`、`RESPONDER`、`ADMIN`
- `phone_masked`
- `community_id`
- `active`

#### `care_subjects` 照护对象

- `id`：主键
- `name`
- `subject_type`：`ELDER` 或 `CHILD`
- `age`
- `gender`
- `household_id`
- `location_text`：模糊位置，不保存精确地址
- `last_check_in_at`
- `active`

#### `households` 家庭

- `id`
- `name`
- `building_text`
- `community_id`

#### `relationships` 关系

- `id`
- `subject_id`
- `user_id`
- `relationship_type`：`FAMILY`、`GUARDIAN`、`PICKUP_AUTHORIZED`
- `can_confirm`
- `can_view`
- `can_pickup`

#### `risk_profiles` 风险档案

- `id`
- `subject_id`
- `tags`
- `check_in_enabled`
- `check_in_interval_hours`
- `pickup_plan_text`
- `notes_safe`

#### `care_events` 照护事件

- `id`
- `event_no`
- `subject_id`
- `event_type`
- `source`
- `risk_level`
- `risk_score`
- `risk_reasons`
- `status`
- `description`
- `occurred_at`
- `created_at`
- `first_response_due_at`
- `escalation_due_at`
- `current_assignee_id`
- `escalation_level`
- `closed_at`
- `closed_by`
- `close_reason`
- `version`：并发更新版本号

#### `assignments` 照护任务

- `id`
- `event_id`
- `assignee_id`
- `resource_id`：可选，第一阶段可以为空
- `assignment_level`
- `status`
- `assigned_at`
- `accepted_at`
- `transferred_at`
- `transfer_reason`

#### `action_logs` 处置记录

- `id`
- `event_id`
- `actor_id`
- `action_type`
- `content`
- `created_at`

#### `notifications` 站内通知

- `id`
- `recipient_id`
- `event_id`
- `notification_type`
- `title`
- `content`
- `read_at`
- `created_at`

### 5.2 索引和约束

至少建立以下索引：

- `care_events(status, created_at)`
- `care_events(risk_level, status)`
- `care_events(subject_id)`
- `care_events(current_assignee_id, status)`
- `notifications(recipient_id, read_at, created_at)`
- `action_logs(event_id, created_at)`

建议加入以下约束：

- 事件编号唯一；
- 关系中的 `subject_id` 和 `user_id` 必须存在；
- 风险等级只能是 `P0`、`P1`、`P2`；
- 状态只能是规定枚举；
- 关闭事件必须有 `closed_at`、`closed_by` 和 `close_reason`；
- 所有时间统一存储为 UTC，接口展示时转换为中国时区。

## 6. 业务规则

### 6.1 事件类型

| 枚举 | 名称 | 最低等级 | 默认响应链 |
|---|---|---|---|
| `ELDER_MISSED_CHECKIN` | 老人长时间未签到 | P1 | 家属/监护人 → 社区工作人员 → 社区资源 |
| `ELDER_HELP` | 老人主动求助 | P1，紧急时 P0 | 家属/监护人 + 社区工作人员 |
| `ELDER_SUSPECTED_FALL` | 疑似老人跌倒或突发异常 | P1 | 家属/监护人 + 社区工作人员 → 社区资源 |
| `CHILD_PICKUP_TIMEOUT` | 儿童接送未确认 | P1 | 监护人 → 托管/社区工作人员 |
| `CHILD_CARE_CHECKIN_ABNORMAL` | 儿童托管签到异常 | P1 | 监护人 → 托管/社区工作人员 |
| `CHILD_HELP` | 儿童主动求助 | P0 | 监护人 + 社区工作人员 |

事件来源：`MANUAL`、`SCHEDULED_CHECKIN`、`SIMULATOR`。

### 6.2 状态机

```text
PENDING → ASSIGNED → IN_PROGRESS → WAITING_CONFIRM → CLOSED
    │          │            │              │
    └──────────┴────────────┴──────────────┴──→ CANCELLED

ASSIGNED 或 IN_PROGRESS 可以发生升级和转派，升级不是独立状态。
```

状态含义：

| 状态 | 含义 |
|---|---|
| `PENDING` | 已创建，尚未形成当前责任人 |
| `ASSIGNED` | 已分配责任人，等待接单 |
| `IN_PROGRESS` | 已有人接单或开始处置 |
| `WAITING_CONFIRM` | 处置已完成，等待家属或授权人员确认 |
| `CLOSED` | 已确认并结束生命周期 |
| `CANCELLED` | 误报、重复或经授权取消，保留审计记录 |

### 6.3 风险分级

```text
风险分 = 事件基础分 + 对象风险标签分 + 未响应分 + 超时分 - 已确认安全分
```

规则：

- P0 基础分 90，P1 基础分 60，P2 基础分 30；
- `独居`、`高龄`、`失能`、`无人监护` 每项加 10 分，最多加 20 分；
- 家属/监护人未响应加 15 分；
- 超过响应时限加 10 分；
- 已完成平安签到或人工确认安全减 20 分；
- 分数大于等于 80 为 P0，50 至 79 为 P1，小于 50 为 P2；
- 结果不得低于该事件类型的最低等级。

详情页必须显示：当前风险等级、风险分、每一项原因和分值变化。

### 6.4 响应时限

| 等级 | 首次接单时限 | 升级时限 |
|---|---:|---:|
| P0 | 1 分钟 | 3 分钟 |
| P1 | 5 分钟 | 10 分钟 |
| P2 | 30 分钟 | 60 分钟 |

正式系统可用定时任务检查真实到期时间；比赛演示必须提供“模拟超时”按钮，直接调用同一套升级用例。

### 6.5 角色权限

| 角色 | 可查看 | 可执行 |
|---|---|---|
| `FAMILY` | 被授权的照护对象、相关事件和处理进度 | 平安签到、发起求助、补充说明、确认关闭 |
| `COMMUNITY_WORKER` | 本社区全部照护对象和事件 | 分级、派单、接单、转派、记录处置、升级、关闭、重置演示数据 |
| `RESPONDER` | 分配给自己的事件和完成任务所需的最少信息 | 接单、反馈、请求转派 |
| `ADMIN` | 全部演示数据和统计 | 演示数据管理及以上操作 |

权限必须在后端执行。前端隐藏按钮只是交互优化，不是安全措施。

## 7. 演示登录与种子数据

### 7.1 演示登录

第一阶段**做登录，不做注册**。

登录页面 `/login` 提供四个预置账号卡片：

1. 家属/监护人；
2. 社区工作人员；
3. 社区响应人；
4. 社区管理员。

点击卡片后调用：

```http
POST /api/demo-login
Content-Type: application/json

{"role": "COMMUNITY_WORKER"}
```

后端根据角色选择预置用户，建立短期演示会话，并设置 HttpOnly 会话 Cookie。响应返回当前用户基本信息和权限摘要。不得允许浏览器自行提交任意 `user_id` 或任意角色绕过后端校验。

登录后顶部可以提供“当前视角”切换，用于比赛演示。切换本质上是重新选择预置演示账号，不代表开放注册或真实身份切换。

不实现：密码、验证码、手机号注册、找回密码、居民自主开户和实名认证。

### 7.2 种子数据

第一次启动或执行重置时写入：

- 四类预置用户；
- 四个照护对象，包含老人和儿童；
- 家庭、家属、监护人和接送授权关系；
- 风险标签、签到计划和安全备注；
- 初始照护事件、任务、时间线和通知。

种子脚本必须幂等：重复执行不会产生重复用户、事件或通知。重置演示数据时应在事务中清理演示数据，再重新插入初始数据。

## 8. REST API 约定

### 8.1 通用约定

- 前缀：`/api`
- 数据格式：JSON
- 时间格式：ISO 8601，数据库存 UTC
- 列表接口统一支持分页和筛选；第一阶段可以使用 `page`、`page_size`
- 写操作返回更新后的资源摘要或详情
- 错误结构统一为：

```json
{
  "code": "EVENT_STATE_CONFLICT",
  "message": "事件已关闭，不能继续处置。",
  "details": {}
}
```

建议使用：`401` 未登录、`403` 无权限、`404` 不存在、`409` 状态冲突、`422` 参数错误。

### 8.2 接口清单

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/api/demo-login` | 演示角色登录 |
| `POST` | `/api/demo-logout` | 清除演示会话 |
| `GET` | `/api/me` | 获取当前用户和权限 |
| `GET` | `/api/people` | 查询当前角色可见的照护对象 |
| `GET` | `/api/people/{id}` | 查看照护对象详情 |
| `POST` | `/api/people/{id}/check-in` | 记录平安签到 |
| `GET` | `/api/events` | 查询事件列表和筛选结果 |
| `POST` | `/api/events` | 创建照护事件 |
| `GET` | `/api/events/{id}` | 查看事件详情和时间线 |
| `POST` | `/api/events/{id}/assign` | 派单或转派 |
| `POST` | `/api/events/{id}/accept` | 接单 |
| `POST` | `/api/events/{id}/actions` | 添加处置记录 |
| `POST` | `/api/events/{id}/simulate-timeout` | 模拟超时并升级 |
| `POST` | `/api/events/{id}/confirm-close` | 家属确认关闭 |
| `POST` | `/api/events/{id}/close` | 授权社区人员关闭 |
| `POST` | `/api/events/{id}/cancel` | 授权取消事件 |
| `GET` | `/api/notifications` | 查询站内通知 |
| `PATCH` | `/api/notifications/{id}/read` | 标记通知已读 |
| `GET` | `/api/dashboard/summary` | 获取看板指标 |
| `POST` | `/api/demo/reset` | 恢复种子数据 |

### 8.3 创建事件示例

```json
{
  "subject_id": "SUBJECT-001",
  "event_type": "ELDER_MISSED_CHECKIN",
  "source": "SIMULATOR",
  "description": "老人连续 4 小时未提交平安签到",
  "urgency": "NORMAL",
  "occurred_at": "2026-09-04T10:00:00+08:00"
}
```

后端创建事件时一次性完成：

1. 保存事件；
2. 计算风险分和原因；
3. 建立初始任务；
4. 生成通知；
5. 写入创建、分级和派单时间线。

### 8.4 查询与命令分离

- `GET` 接口只负责查询，不改变业务状态；
- `POST` 命令接口负责状态变化；
- 看板使用只读查询，不在前端从事件列表自行推导关键业务指标；
- 后端返回的事件详情应包含当前责任人、风险原因、时限、任务历史和时间线。

## 9. 前端架构

### 9.1 页面路由

```text
/login
/events
/events/:id
/people
/people/:id
/simulator
/dashboard
```

页面必须根据产品文档重新设计。当前原型可以帮助理解流程，但不要求保留其布局、CSS、颜色或组件。

### 9.2 前端职责

- 展示后端返回的数据；
- 进行表单输入和基础格式校验；
- 发起命令请求并处理成功、失败、权限和冲突状态；
- 展示风险原因、时间线、当前责任人和响应时限；
- 保存筛选、分页和临时 UI 状态；
- 不在浏览器中实现最终风险分级、权限判断或状态流转。

### 9.3 建议的页面模块

- `auth`：登录页、会话恢复和退出；
- `events`：事件列表、筛选栏、事件详情、操作面板；
- `subjects`：照护对象列表、档案和关系信息；
- `simulator`：六类场景和模拟超时；
- `dashboard`：事件总数、风险结构、响应时间、超时率和关闭趋势；
- `shared`：表格、状态徽章、确认弹窗、错误提示和空状态。

### 9.4 交互状态

每个写操作都必须有：

- 提交中状态；
- 成功反馈；
- 权限错误反馈；
- 状态冲突反馈；
- 网络或服务器错误反馈；
- 成功后刷新相关查询缓存。

## 10. 事务、调度和通知

### 10.1 事务边界

以下操作必须在同一个事务中完成：

```text
接单 = 更新事件 + 更新任务 + 写入时间线
转派 = 更新当前责任人 + 新建任务历史 + 写入原因 + 通知新责任人
升级 = 更新升级次数和责任人 + 写入升级记录 + 创建通知
关闭 = 更新关闭字段 + 写入关闭记录
```

事务失败时整体回滚，不能出现“页面显示已接单但时间线没有记录”的半完成状态。

### 10.2 超时调度

- 单进程定时任务每隔几秒或几十秒扫描到期事件；
- 只处理仍处于可升级状态的事件；
- 升级操作必须幂等，重复扫描不能重复增加升级次数或重复发送同一升级通知；
- `simulate-timeout` 直接调用同一个升级用例，不复制另一套逻辑；
- 调度器异常不能阻止人工操作和模拟操作。

### 10.3 通知

第一阶段只做数据库站内通知：

- 通知接收人；
- 通知标题、正文、事件编号；
- 创建时间和已读时间；
- 页面未读数；
- 全部标记已读。

不承诺短信、微信或电话送达。消息送达和责任人实际处理是两个不同状态。

## 11. 安全、隐私和可靠性

- 所有数据访问先经过后端角色和关系授权；
- 家属只能看到被授权的照护对象；
- 响应人只看到完成当前任务所需的最少信息；
- 只展示脱敏电话和模糊位置；
- 不保存身份证号、详细病历、精确家庭住址或视频；
- 事件时间线由服务端生成，不能接受客户端伪造的操作人和时间；
- 日志不得打印完整手机号、会话 Cookie 或其他敏感数据；
- 关闭和取消不删除事件，必须保留审计记录；
- 使用数据库事务和版本号避免并发覆盖；
- 风险判断文案统一使用“疑似异常”“建议人工确认”，不能使用“确诊”或“百分百识别”。

## 12. 测试方案

### 12.1 后端领域测试

至少覆盖：

- 风险基础分、标签加分、未响应加分、超时加分和确认安全减分；
- 事件类型最低风险等级；
- 各角色查看范围；
- 合法和非法状态流转；
- P0 不被普通响应人降级；
- 关闭前必须有处置记录；
- 转派原因不能为空；
- 升级幂等；
- 已关闭事件只读。

### 12.2 API 集成测试

- 演示登录和会话恢复；
- 家属访问未授权对象返回 `403` 或不可见；
- 创建事件后事件、任务、通知和时间线同时存在；
- 接单、转派、处置、升级和关闭的事务一致性；
- 重置后恢复到种子数据；
- SQLite 临时数据库迁移可重复执行。

### 12.3 前端和端到端测试

至少有两条 Playwright 流程：

**老人未签到闭环**

```text
演示登录 → 模拟老人未签到 → 查看 P1 原因
→ 切换家属 → 模拟超时/确认平安
→ 切换社区工作人员 → 接单并记录处置
→ 切换家属 → 确认关闭
```

**儿童接送闭环**

```text
演示登录 → 创建或模拟儿童接送未确认
→ 查看监护关系和责任人 → 接单/转派
→ 记录接送结果 → 监护人确认关闭
```

### 12.4 文档和交付检查

- `node` 或 TypeScript 语法检查通过；
- 前端生产构建通过；
- 后端 pytest 全部通过；
- Playwright 核心流程通过；
- Markdown 文件为 UTF-8 无 BOM；
- `git diff --check` 无空白错误；
- 本地无外部网络和硬件时仍能完成演示。

## 13. 建议项目结构

```text
项目根目录/
├── backend/
│   ├── app/
│   ├── migrations/
│   ├── tests/
│   ├── pyproject.toml
│   └── seed/
├── frontend/
│   ├── src/
│   │   ├── app/
│   │   ├── modules/
│   │   ├── components/
│   │   └── services/
│   ├── tests/
│   ├── package.json
│   └── vite.config.ts
├── prototype/             # 仅作交互参考，可独立保留
├── 邻里智护第一阶段产品设计方案.md
├── 邻里智护第一阶段技术架构方案.md
├── 邻里智护竞品调研.md
├── CONTEXT.md
└── start.ps1
```

## 14. 实施顺序

### 第 1 步：后端基础

- 建立 FastAPI 工程；
- 配置 SQLite 和 Alembic；
- 创建全部核心表；
- 编写种子数据和重置命令；
- 实现演示登录、会话和角色权限；
- 为模型和迁移添加测试。

### 第 2 步：领域事件引擎

- 实现事件枚举和状态机；
- 实现风险计算策略；
- 实现创建、接单、转派、处置、签到、升级、关闭和取消；
- 实现时间线、通知和事务；
- 完成领域和 API 测试。

### 第 3 步：查询 API

- 实现照护对象和事件列表；
- 实现筛选、排序、分页和详情；
- 实现通知列表和看板摘要；
- 对越权和状态冲突补充测试。

### 第 4 步：全新前端

- 根据产品文档设计 `/login`；
- 实现事件中心、对象档案、详情、模拟台和看板；
- 对接 REST API；
- 实现加载、空状态、错误、权限和冲突反馈。

### 第 5 步：自动升级与演示打磨

- 加入轻量定时调度；
- 保留模拟超时入口；
- 完成老人和儿童两条 Playwright 流程；
- 提供一条命令启动和一键重置；
- 按产品设计方案验收清单逐项验证。

## 15. 给开发 AI 的可直接使用的任务描述

```text
请根据以下文件实现邻里智护第一阶段正式系统：

1. CONTEXT.md
2. 邻里智护第一阶段产品设计方案.md
3. 邻里智护第一阶段技术架构方案.md

请采用 Vue 3 + TypeScript + Vite 前端、FastAPI + Pydantic + SQLAlchemy/SQLModel 后端和 SQLite 数据库，构建一个前后端分离的模块化单体应用。

先完成数据库模型、Alembic 迁移、种子数据、演示登录、角色权限和 REST API，再实现页面。第一阶段要完成照护对象、家庭关系、六类照护事件、P0/P1/P2 可解释风险分级、派单、接单、转派、处置时间线、模拟超时升级、站内通知、平安签到、家属确认关闭、授权社区人员关闭、统计看板和一键重置。

正式前端必须重新设计。prototype/ 只作为业务流程、种子数据和文案参考，不要直接复制其 HTML、CSS 或 JavaScript。

第一阶段做演示登录，不做开放注册，不接入摄像头、手环、定位器、门禁、MQTT、Zigbee、短信、微信、真实电话或医疗诊断。

所有状态变化必须由后端事件引擎完成，并在同一个事务中更新事件、任务、处置记录、时间线和通知。风险原因必须可解释，已关闭事件必须只读。完成后补充 pytest、Vitest 和 Playwright 测试，并按产品文档的验收标准逐条验证。
```

## 16. 完成定义

当下面条件全部满足时，才算完成第一阶段：

- 启动系统能看到虚构用户、照护对象和初始事件；
- `/login` 可以选择四个预置演示角色；
- 家属、社区工作人员和响应人看到的范围不同；
- 模拟老人未签到后能看到风险分和计算原因；
- 超时升级会产生升级记录、责任人变化和站内通知；
- 社区工作人员能接单、转派和记录处置；
- 事件可以进入待确认并由授权人员关闭；
- 关闭后时间线完整且事件只读；
- 儿童接送场景能复用同一套事件闭环；
- 看板数据会随操作更新；
- 重置后恢复初始状态；
- 无硬件、无外部账号、无网络服务时仍能完成演示；
- 自动化测试和编码检查全部通过。
